// PLANNING
// attacker wins %
// defender wins %
// average troops left
// calculate results here? Or elsewhere?
// if calculate results here, then take in formValues. Then this component would be "smart" and do the calculations
// if calculate results elsewhere, take in Results. THen this component would be "dumb" and just display results

const Results = () => {};

export default Results;
